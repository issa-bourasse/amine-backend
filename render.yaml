services:
  - type: web
    name: amine-backend
    env: node
    region: oregon
    buildCommand: npm install && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: MONGO_URI
        sync: false
      - key: PORT
        value: 3001
      - key: CLIENT_ORIGIN
        sync: false
